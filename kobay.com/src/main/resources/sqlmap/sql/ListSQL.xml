<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd" >
<sqlMap>
	<typeAlias alias="listVO" type="kobay.com.service.ListVO"/>
	<typeAlias  alias="egovMap" type="egovframework.rte.psl.dataaccess.util.EgovMap"/>
	
	<select id="listDAO.selectList" parameterClass="listVO" resultClass="egovMap"> <!-- 진행중 -->
		SELECT memberUnq, auctionUnq, LCTG, MCTG, to_char(sDate,'yyyy/mm/dd  hh24:mi:ss') sDate,
				to_char(eDate,'yyyy/mm/dd hh24:mi') eDate,aucTitle, sPrice, ePrice, aucImageMain
			FROM KOBAYAUCTION  WHERE 1=1
				ORDER BY auctionUnq ASC
	</select>
	
	<select id="listDAO.selectList2" parameterClass="listVO" resultClass="egovMap"> <!-- 진행예정 -->
		select memberUnq, auctionUnq, LCTG, MCTG, to_char(sDate,'yyyy/mm/dd hh24:mi:ss') sDate,
				to_char(eDate,'yyyy/mm/dd hh24:mi') eDate, aucTitle, sPrice, ePrice
			from KOBAYAUCTION
	</select>
	
	<select id="listDAO.selectList3" parameterClass="listVO" resultClass="egovMap"> <!-- 마감 or 낙찰 -->
		select memberUnq, auctionUnq, LCTG, MCTG, to_char(sDate,'yyyy/mm/dd hh24:mi:ss') sDate,
				to_char(eDate,'yyyy/mm/dd hh24:mi') eDate, aucTitle, sPrice, ePrice
			from KOBAYAUCTION
	</select> 
	
<!-- 	<select id="listDAO.selectListArray" resultClass="egovMap"> 정렬 미완성
		select title,auction_price,to_char(sdate,'yyyy/mm/dd hh24:mi:ss') sdate   
			from KOBAYAUCTION
			<isEqual property="arrayType" compareValue="newest">
				order by sdate desc
			</isEqual>
			<isEqual property="arrayType" compareValue="lowprice">
				order by sprice asc
			</isEqual>
			<isEqual property="arrayType" compareValue="highprice">
				order by sprice desc
			</isEqual>
	</select> -->
	
	<select id="listDAO.selectListTotal" resultClass="java.lang.Integer">
		select count (*) from KOBAYAUCTION where 1=1
	</select>
	
	<select id="listDAO.selectAucImageMain" parameterClass="listVO" resultClass="egovMap">
		SELECT auctionUnq,aucImageMain FROM KOBAYAUCTION 
			WHERE 1=1 ORDER BY auctionUnq ASC
	</select>
	
</sqlMap>